---
title: Portfolio of Mark McLaughlin
eleventyNavigation:
  key: Projects
abstract: "Mark McLaughlin is a English-born Irish designer and developer, currently employed as a Principal Design Manager by Microsoft in Seattle."
---
{% extends "base.njk" %}

{% from "helpers/lazysizes.njk" import picture %}

{% block content %}
    {% set mobile = "d-xs-block d-sm-block d-md-none d-lg-none" %}
    {% set tablet = "d-none d-md-block d-lg-block" %}

    <div class="container-fluid pt-3 bg-light {{tablet}}">
        <div class="container h-50 py-5">
            <div class="row">
                <div class="col-md-12 d-inline-block pt-3 pt-md-5">
                    <h2 class="display-4 text-dark mt-3 ">Hi! My name is Mark.</h2>
                    <h3 class="h1 text-secondary mb-4">I've designed <span class="typed text-info"></span></h3>
                </div>
            </div>
        </div>
    </div>

    <div class="container my-5 pt-3 pt-md-0">
        <div class="row mt-3 mt-md-0">
            {% for page in collections.projects | reverse  %}
            <!-- Hero  -->
                {% if loop.index >= 1 and loop.index <= 4  %}
                    {% set classes = "col-md-6 mb-4" %}
                    {% set width = 800 %}
                {% elif loop.index >= 4  %}
                    {% set classes = "col-lg-4 col-md-6 mb-3" %}
                    {% set width = 800 %}
                {% endif %}
                <div class="{{ classes }}">
                    <a href="{{page.url}}index.html">
                        {{ picture(page.data.title, page.data.client, width ) }}
                    </a>
                    <h3 class="h4 mt-4"><a href="{{page.url}}" class="text-info">{{page.data.title}}</a></h3>
                    <p class="text-secondary">{{ page.data.abstract }}</p>
                </div>
            <!-- // hero -->
            {% endfor %}
        </div>
    </div> <!-- // row -->
</div> <!-- container -->
<script>
    var typed = new Typed('.typed', {
    strings: [
            'some of the most loved developer tools.', 
            "video calling for Xbox gamers.",
            "sites for Grammy-winning artists.",
            'augmented reality for astronauts.', 
            "the most downloaded iPad app of all time.",
        ],
        smartBackspace: false,
        loop: true,
        startDelay: 1000,
        typeSpeed: 66,
        backDelay: 2500,
        fadeOut: true
    });
</script>
{% endblock %}