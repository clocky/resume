{
  "name": "json-resume",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "watch:sass": "sass  --no-source-map --watch src/sass:dist/css",
    "watch:eleventy": "eleventy --serve",
    "build:sass": "sass  --no-source-map src/sass:dist/css",
    "build:eleventy": "eleventy",
    "postbuild": "lightningcss --minify --targets '> 0.5%, not IE 11' dist/css/style.css -o dist/css/style.css",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:sass build:eleventy"
  },
  "author": "clocky",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^1.0.2",
    "@picocss/pico": "^1.5.5",
    "cross-env": "^7.0.3",
    "dayjs": "^1.11.5",
    "lightningcss-cli": "^1.15.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.54.9"
  },
  "browserslist": [
    "> 0.25%",
    "not IE 11"
  ],
  "devDependencies": {
    "normalize.css": "^8.0.1",
    "prettier": "^2.7.1"
  }
}
